---
import { apiCategories, type ApiCategory, type ApiItem } from "~/lib/apiCategories";

interface Props {
  currentPath?: string;
}

const { currentPath } = Astro.props;
---

<aside class="w-64 bg-base-200 min-h-screen p-4">
  <div class="mb-6">
    <a href="/" class="btn btn-ghost btn-block justify-start">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
      </svg>
      首页
    </a>
  </div>

  <div class="menu">
    {apiCategories.map((category: ApiCategory) => (
      <div class="mb-4">
        <div class="menu-title flex items-center gap-2">
          <span>{category.icon}</span>
          <span>{category.name}</span>
        </div>
        <ul>
          {category.items.map((item: ApiItem) => (
            <li>
              <a
                href={item.path}
                class={currentPath === item.path ? "active" : ""}
              >
                {item.name}
              </a>
            </li>
          ))}
        </ul>
      </div>
    ))}
  </div>

  <div class="divider"></div>

  <div class="text-xs text-base-content/60 space-y-1">
    <div class="flex items-center gap-2">
      <span>🦀</span>
      <span>Rust + WASM</span>
    </div>
    <div class="flex items-center gap-2">
      <span>⚡</span>
      <span>高性能</span>
    </div>
  </div>
</aside>
