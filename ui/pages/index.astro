---
import Layout from "~/layouts/Layout.astro";
import { apiCategories } from "~/lib/apiCategories";
---

<Layout title="WASM Toolkit - 高性能 Web 工具库">
  <div class="min-h-screen bg-gradient-to-br from-base-200 to-base-300">
    <!-- Hero Section -->
    <div class="hero min-h-[60vh] bg-base-100">
      <div class="hero-content text-center">
        <div class="max-w-3xl">
          <h1 class="text-5xl md:text-6xl font-bold mb-6">
            🦀 WASM Toolkit
          </h1>
          <p class="text-xl md:text-2xl mb-8 text-base-content/80">
            基于 Rust 和 WebAssembly 构建的高性能 Web 工具库
          </p>
          <div class="flex flex-wrap justify-center gap-4 mb-8">
            <div class="badge badge-lg badge-primary">高性能</div>
            <div class="badge badge-lg badge-secondary">类型安全</div>
            <div class="badge badge-lg badge-accent">零依赖</div>
            <div class="badge badge-lg badge-info">跨平台</div>
          </div>
          <div class="flex flex-wrap justify-center gap-4">
            <a href="#apis" class="btn btn-primary btn-lg">
              查看 API 文档
            </a>
            <a
              href="https://github.com/yourusername/wasm-toolkit"
              class="btn btn-outline btn-lg"
              target="_blank"
              rel="noopener"
            >
              GitHub
            </a>
          </div>
        </div>
      </div>
    </div>

    <!-- Features Section -->
    <div class="py-20 px-4">
      <div class="max-w-6xl mx-auto">
        <h2 class="text-3xl font-bold text-center mb-12">核心特性</h2>
        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
          <div class="card bg-base-100 shadow-xl">
            <div class="card-body items-center text-center">
              <div class="text-5xl mb-4">⚡</div>
              <h3 class="card-title">高性能</h3>
              <p>WebAssembly 提供接近原生的执行速度，性能是纯 JavaScript 的数倍</p>
            </div>
          </div>

          <div class="card bg-base-100 shadow-xl">
            <div class="card-body items-center text-center">
              <div class="text-5xl mb-4">🔒</div>
              <h3 class="card-title">类型安全</h3>
              <p>Rust 强大的类型系统确保运行时安全，减少错误</p>
            </div>
          </div>

          <div class="card bg-base-100 shadow-xl">
            <div class="card-body items-center text-center">
              <div class="text-5xl mb-4">📦</div>
              <h3 class="card-title">零依赖</h3>
              <p>无需额外依赖库，体积小巧，开箱即用</p>
            </div>
          </div>

          <div class="card bg-base-100 shadow-xl">
            <div class="card-body items-center text-center">
              <div class="text-5xl mb-4">🌐</div>
              <h3 class="card-title">跨平台</h3>
              <p>在所有现代浏览器中运行，无需额外配置</p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- API Categories Section -->
    <div id="apis" class="py-20 px-4 bg-base-100">
      <div class="max-w-6xl mx-auto">
        <h2 class="text-3xl font-bold text-center mb-4">API 文档</h2>
        <p class="text-center text-base-content/70 mb-12 max-w-2xl mx-auto">
          选择下方分类查看详细的 API 文档和在线演示
        </p>

        <div class="space-y-12">
          {apiCategories.map((category) => (
            <div>
              <div class="flex items-center gap-3 mb-6">
                <span class="text-4xl">{category.icon}</span>
                <h3 class="text-2xl font-bold">{category.name}</h3>
                <div class={`badge badge-${category.color}`}>
                  {category.items.length} 个 API
                </div>
              </div>

              <div class="grid md:grid-cols-2 gap-6">
                {category.items.map((item) => (
                  <a href={item.path} class="group">
                    <div class="card bg-base-200 hover:bg-base-300 transition-all duration-200 hover:shadow-xl group-hover:-translate-y-1">
                      <div class="card-body">
                        <h4 class="card-title text-xl group-hover:text-primary transition-colors">
                          {item.name}
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            class="h-5 w-5 opacity-0 group-hover:opacity-100 transition-opacity"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke="currentColor"
                          >
                            <path
                              stroke-linecap="round"
                              stroke-linejoin="round"
                              stroke-width="2"
                              d="M9 5l7 7-7 7"
                            />
                          </svg>
                        </h4>
                        <p class="text-base-content/70">{item.description}</p>
                        <div class="card-actions justify-end mt-4">
                          <div class="badge badge-outline">文档</div>
                          <div class="badge badge-outline">演示</div>
                        </div>
                      </div>
                    </div>
                  </a>
                ))}
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>

    <!-- Installation Section -->
    <div class="py-20 px-4">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-3xl font-bold text-center mb-12">快速开始</h2>

        <div class="space-y-6">
          <div class="card bg-base-100 shadow-xl">
            <div class="card-body">
              <h3 class="card-title">安装</h3>
              <div class="mockup-code">
                <pre data-prefix="$"><code>npm install wlin-toolkit</code></pre>
              </div>
            </div>
          </div>

          <div class="card bg-base-100 shadow-xl">
            <div class="card-body">
              <h3 class="card-title">使用示例</h3>
              <div class="mockup-code">
                <pre data-prefix="1"><code>import &#123; digitUppercase, replaceUndefinedWithNull &#125; from 'wlin-toolkit';</code></pre>
                <pre data-prefix="2"><code></code></pre>
                <pre data-prefix="3"><code>// 数字转大写</code></pre>
                <pre data-prefix="4"><code>const result = digitUppercase(123.45);</code></pre>
                <pre data-prefix="5"><code>console.log(result); // "壹佰贰拾叁元肆角伍分"</code></pre>
                <pre data-prefix="6"><code></code></pre>
                <pre data-prefix="7"><code>// 替换 undefined</code></pre>
                <pre data-prefix="8"><code>const cleaned = replaceUndefinedWithNull(&#123; name: "John", age: undefined &#125;);</code></pre>
                <pre data-prefix="9"><code>console.log(cleaned); // &#123; name: "John", age: null &#125;</code></pre>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <footer class="footer footer-center p-10 bg-base-200 text-base-content">
      <div>
        <div class="grid grid-flow-col gap-4">
          <span class="text-sm opacity-70">
            Built with Rust 🦀 + WebAssembly ⚡ + Astro 🚀
          </span>
        </div>
        <div class="mt-4">
          <p class="text-sm opacity-70">© 2024 WASM Toolkit. All rights reserved.</p>
        </div>
      </div>
    </footer>
  </div>
</Layout>
